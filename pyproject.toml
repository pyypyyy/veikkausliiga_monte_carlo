[project]
name = "veikkausliiga-monte-carlo-sim"
version = "0.1.0"
description = "Monte Carlo simulator for Veikkausliiga regular-season standings"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.26",
  "pandas>=2.1",
  "openpyxl>=3.1",
]

[tool.setuptools.packages.find]
where = ["src"]
